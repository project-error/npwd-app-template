{
  "name": "npwd-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs && cd web && npx vite build",
    "build:game": "node scripts/build.mjs",
    "build:web": "cd web && npx vite build",
    "dev": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "@citizenfx/client": "^2.0.7061-1",
    "@citizenfx/server": "^2.0.7061-1",
    "esbuild": "^0.24.0",
    "typescript": "^5.4.5"
  }
}
